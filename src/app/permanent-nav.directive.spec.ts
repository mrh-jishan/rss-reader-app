import { PermanentNavDirective } from './permanent-nav.directive';

describe('PermanentNavDirective', () => {
  it('should create an instance', () => {
    const directive = new PermanentNavDirective();
    expect(directive).toBeTruthy();
  });
});
